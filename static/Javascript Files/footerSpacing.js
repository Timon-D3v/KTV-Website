/*
let body = document.querySelector("body");
let bodyHeight = body.clientHeight;
let bodyWidth = body.clientWidth;
let windowHeight = window.innerHeight;
if (bodyWidth < windowHeight) {
    bodyHeight += 256;
} else {
    bodyHeight += 128;
};
let style = "top: " + bodyHeight + "px;";
document.getElementById("footer").style = style;

window.addEventListener("resize", function(){
    let body = document.querySelector("body");
    let bodyHeight = body.clientHeight;
    let bodyWidth = body.clientWidth;
    let windowHeight = window.innerHeight;
    if (500 < bodyWidth < windowHeight) {
        bodyHeight += 256;
    } else {
        bodyHeight += 128;
    };
    let style = "top: " + bodyHeight + "px;";
    document.getElementById("footer").style = style;
});*/